import 'package:flutter/material.dart';

/// Application color palette
/// 
/// Defines all color constants used throughout the application.
class AppColors {
  /// Primary brand color (purple)
  static const Color primaryColor = Color(0xFF5932EA);
  
  /// Background color (very light purple)
  static const Color backgroundColor = Color(0xFFFCFAFF);
  
  /// Sidebar color (light pink)
  static const Color sidebarColor = Color(0xFFF7EDEE);
  
  /// Dark text color
  static const Color textDark = Color(0xFF444444);
  
  /// Light text color (white)
  static const Color textLight = Color(0xFFFFFFFF);
  
  /// Success color (green)
  static const Color successColor = Color(0xFF2ECC71);
  
  /// Danger/error color (red)
  static const Color dangerColor = Color(0xFFE74C3C);
  
  /// Icon color
  static const Color iconColor = Color(0xFF444444);
  
  /// Primary text color
  static const Color textPrimary = Color(0xFF444444);
  
  /// Card background color (subtle lavender)
  static const Color cardBackground = Color(0xFFF4F0FF);
  
  /// Accent color (lighter purple)
  static const Color accentColor = Color(0xFF7B56F9);
  
  /// Secondary text color (gray)
  static const Color textSecondary = Color(0xFF888888);
}

